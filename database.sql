-- STEP 1: To create a database, you can choose a suitable name, such as moviesdb1. 
-- The code required to create the database can be written in the terminal. 
-- The image displayed represents the necessary code.

CREATE database moviesdb;
use moviesdb;