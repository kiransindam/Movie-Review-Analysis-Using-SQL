create table movies(
movie_id int,
title varchar(255),
industry varchar(255),
imdb_rating int,
studio varchar(255),
language_id int
);

-- view the data using the select function in give below 

Select * from movies